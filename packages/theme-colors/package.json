{
  "name": "@vscode-theme-inspector/theme-colors",
  "version": "0.0.1",
  "private": true,
  "description": "Registry of VS Code Theme Color IDs (id, category, description), generated from the official reference.",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^4.1.11"
  }
}
